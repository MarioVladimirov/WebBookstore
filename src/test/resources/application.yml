spring:
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: create-drop

cloudinary:
  cloud-name: nzlateva
  api-key: 365111573122239
  api-secret: ${CLOUDINARY_SECRET}

schedulers:
  cron: "0 2 */1 * * *"
